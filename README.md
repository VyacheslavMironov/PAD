# PAD
PAD - это электронный журнал общего назначения.

npm install -g typescript
npm install -g vue@next
npm install -g axios

Пример компиляции TypeScript - tsc --outDir scripts\js\ scripts\ts\signup_form.ts